// Arrays
int intArray[5];
Person personArray[5]; // 5 person objects

int search (int arr[], int size, int num){
	for (int i = 0; i < size; i++){
		if(arr[i] == num){
			return i;
		}
	}
}

if(arr[i].getnum() == num) // public member function of the person class.

//Dynamic arrays
int * arr = new int[5];
delete [] arr;  // need to delete pointer and return to heap. 
